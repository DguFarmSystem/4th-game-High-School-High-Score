using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using Stage;

public class ClassRoomStage : StageNormal
{
    /*
    BlockScript[] allBlocks = FindObjectsOfType<BlockScript>();
    GameObject[] allBlocks = GameObject.FindAllGameObjectsWithTag("BlockTag");
    BlockScript selectedBlock = allBlocks[Random.Range(0, allBlocks.Length)];
    */

    // ============ Lifecycle methods ============ //
    void Start()
    {
        
    }

    
    void Update()
    {
        
    }
}
